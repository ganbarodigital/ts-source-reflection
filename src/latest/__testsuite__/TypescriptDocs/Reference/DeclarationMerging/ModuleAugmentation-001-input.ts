// consumer.ts
import { Observable } from "./ModuleAugmentation-001-observable";
import "./ModuleAugmentation-001-map";

// we'll check the definition of this type when we do the
// Inferred and Reflection layers of the library
type ForInspection = Observable<number>;